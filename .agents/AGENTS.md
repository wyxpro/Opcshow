# Workspace Rules

## 自动代码提交与同步规则 (Auto Git Commit & Sync Rule)
- 每次在完成代码修改与功能实现后，必须自动执行 Git 提交与同步（`git add .`、`git commit`，以及 `git push`/`git status` 确认）。
- 所有 Git 提交记录的描述 (Commit Message) 必须使用**中文**进行清晰规范的撰写，总结本次修改的主要功能与变更点。
